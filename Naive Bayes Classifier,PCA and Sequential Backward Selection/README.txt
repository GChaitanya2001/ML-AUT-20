**************************************************************************************
The zip file consists of a .ipynb file and a report detailing the procedure and results.

Run .ipynb file on any python platform, preferably GOOGLE COLAB.

* While running on colab upload the dataset in  ' /content/sample_data/Train_A.csv '
(try changing the string inside pd.readcsv('') manually, if FileNotFound error occurs) 

* Otherwise, change the string manually inside ' pd.readcsv('<< add the path here <<') ' 
   to the path of dataset in the first line of main() function.

Install the libraries mentioned(if not installed) on top of .ipynb file
 
Instructions for installing libraries:

* Use: 
       pip install /libraryname/


****************************************************************************************